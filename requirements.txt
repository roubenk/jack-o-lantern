# Runtime dependencies for the Pi (index.py).
# The cloud function has its own cloud_function/requirements.txt.
gpiozero>=1.6.2
PyAudio>=0.2.11
PyYAML>=6.0
requests>=2.28.0
# Pi-only: needs a C build and real WS281x hardware, so it is skipped on x86
# dev machines and CI. led_animations.py is the only consumer, and it only ever
# runs on the lantern itself.
rpi-ws281x>=4.3.0; platform_machine in "armv6l armv7l aarch64"
SpeechRecognition>=3.10.0
